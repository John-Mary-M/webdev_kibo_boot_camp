/**
 * 19/07/2023
 * while loops
 */

const colours = ["Red", "Blue", "Green", "Orange", "Purple"];
let colour = 0;
while (colour < colours.length) {
        // console.log(`${colours[colour]}`); same as the line below
        console.log(colours[colour])
        colour++;
}